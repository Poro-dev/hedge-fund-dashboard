# Database (Neon project: poro-dev, project_id: square-recipe-80811889)
DATABASE_URL=           # Pooled: get from console.neon.tech → poro-dev → Connection Details → Pooled
DATABASE_URL_DIRECT=    # Direct: get from console.neon.tech → poro-dev → Connection Details → Direct

# Auth — Auth.js v5
AUTH_SECRET=            # Random string: run `openssl rand -base64 32`
AUTH_URL=               # http://localhost:3000 (dev) or https://yourdomain.com (prod)
GOOGLE_CLIENT_ID=       # From Google Cloud Console → OAuth 2.0 credentials
GOOGLE_CLIENT_SECRET=   # From Google Cloud Console → OAuth 2.0 credentials

ALLOWED_EMAIL=          # Your Google account email — only this address can sign in

# Add project-specific vars below
